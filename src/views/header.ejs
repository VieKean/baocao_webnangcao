<div class="header-background d-flex justify-content-between align-items-center mb-3">
    <% if (data.username) { %>
        <h3>Welcome, <%= data.username %>!</h3>
        <a href="/logout" class="btn btn-primary">Logout</a>
    <% } %>
</div>